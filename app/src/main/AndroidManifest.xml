<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.monitoreoconsumodelhogar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Actividad de splash screen -->
        <activity android:name=".UserInterface.main.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de inicio de sesión (Login) -->
        <activity android:name=".UserInterface.auth.AccessActivity"
            android:exported="true" />

        <activity android:name=".UserInterface.main.WelcomeActivity"
            android:exported="true" />

        <!-- MainActivity (después de iniciar sesión) -->
        <activity android:name=".UserInterface.main.MainActivity"
            android:exported="true" />

        <!-- Actividades adicionales -->
        <activity android:name=".UserInterface.rooms.CreateRoomActivity" />
        <activity android:name=".UserInterface.rooms.CreateHallActivity" />
        <activity android:name=".UserInterface.rooms.ViewRoomsActivity" />
        <activity android:name=".UserInterface.graphs.GraphActivity" /> <!-- Añadido para la actividad del gráfico -->

    </application>
</manifest>
