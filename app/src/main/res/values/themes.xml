<resources>

    <!-- Tema oscuro -->
    <style name="Theme.MonitoreoConsumoDelHogar" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Colores principales para el tema oscuro -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item> <!-- Cambiado a colorPrimaryDark -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="android:colorBackground">@color/black</item>

        <!-- Personalización de la barra de acciones -->
        <item name="android:windowActionBar">true</item>
        <item name="android:windowActionBarOverlay">true</item>
    </style>

</resources>
